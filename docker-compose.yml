services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  # 로컬 PostgreSQL (현재는 Neon 클라우드 사용 중 — 로컬 DB로 전환 시 주석 해제)
  # db:
  #   image: postgres:15
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_USER: nodevelture
  #     POSTGRES_PASSWORD: nodevelture
  #     POSTGRES_DB: nodevelture
  #   ports:
  #     - "5433:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U nodevelture"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

volumes:
  redis_data:
  # postgres_data:
